{
  "name": "practice_nodejsexpress",
  "version": "0.0.1",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "shx rm -rf dist && cd ../frontend && npm install && npm run build && shx cp -r dist ../backend"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "shx": "^0.4.0"
  }
}
